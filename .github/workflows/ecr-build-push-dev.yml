name: Build & Deploy Dev (ECR -> ECS)

on:
  push:
    branches: ["dev"]
    paths:
      - "backend/Gateway.Api/**"
      - "backend/Web.Bff.Api/**"
      - "backend/Auth.Api/**"
      - "deploy/ecs/**"
      - ".github/workflows/**"

permissions:
  id-token: write
  contents: read

env:
  AWS_REGION: us-east-1
  AWS_ACCOUNT_ID: "373268394585"
  ROLE_ARN: "arn:aws:iam::373268394585:role/github-actions-ecr-push"

  ECS_CLUSTER_DEV: "homesite-dev"
  ECS_SERVICE_AUTH_DEV: "homesite-auth-dev-service"
  ECS_SERVICE_BFF_DEV: "homesite-webbff-dev-service"
  ECS_SERVICE_GATEWAY_DEV: "homesite-gateway-dev-service"

jobs:
  detect:
    runs-on: ubuntu-latest
    outputs:
      gateway: ${{ steps.filter.outputs.gateway }}
      bff: ${{ steps.filter.outputs.bff }}
      auth: ${{ steps.filter.outputs.auth }}
    steps:
      - uses: actions/checkout@v4
      - id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            gateway:
              - 'backend/Gateway.Api/**'
              - 'deploy/ecs/dev/gateway-taskdef.json'
            bff:
              - 'backend/Web.Bff.Api/**'
              - 'deploy/ecs/dev/web-bff-taskdef.json'
            auth:
              - 'backend/Auth.Api/**'
              - 'deploy/ecs/dev/auth-taskdef.json'

  build_push_deploy:
    runs-on: ubuntu-latest
    needs: detect
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: gateway
            changed: ${{ needs.detect.outputs.gateway }}
            ecr_repo: homesite/gateway-api
            dockerfile: Gateway.Api/Dockerfile
            taskdef: deploy/ecs/dev/gateway-taskdef.json
            container_name: gateway
            ecs_service_dev: homesite-gateway-dev-service

          - name: web-bff
            changed: ${{ needs.detect.outputs.bff }}
            ecr_repo: homesite/web-bff-api
            dockerfile: Web.Bff.Api/Dockerfile
            taskdef: deploy/ecs/dev/web-bff-taskdef.json
            container_name: web-bff
            ecs_service_dev: homesite-webbff-dev-service

          - name: auth
            changed: ${{ needs.detect.outputs.auth }}
            ecr_repo: homesite/auth-api
            dockerfile: Auth.Api/Dockerfile
            taskdef: deploy/ecs/dev/auth-taskdef.json
            container_name: auth
            ecs_service_dev: homesite-auth-dev-service

    steps:
      - uses: actions/checkout@v4

      - name: Debug ref
        run: |
          echo "ref_name=${{ github.ref_name }}"
          echo "ref=${{ github.ref }}"
          echo "repo=${{ github.repository }}"
          echo "sha=${{ github.sha }}"

      - name: Set up Docker Buildx
        if: matrix.changed == 'true'
        uses: docker/setup-buildx-action@v3

      - name: Configure AWS credentials (OIDC)
        if: matrix.changed == 'true' && github.ref_name == 'dev'
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ env.ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to ECR
        if: matrix.changed == 'true' && github.ref_name == 'dev'
        uses: aws-actions/amazon-ecr-login@v2

      - name: Compute tags
        if: matrix.changed == 'true'
        id: vars
        shell: bash
        run: |
          SHA_SHORT="${GITHUB_SHA::7}"
          SAFE_REF="${GITHUB_REF_NAME//\//-}"
          echo "sha_short=$SHA_SHORT" >> "$GITHUB_OUTPUT"
          echo "image_tag=${SAFE_REF}-${SHA_SHORT}" >> "$GITHUB_OUTPUT"

      - name: Build & push - ${{ matrix.name }}
        if: matrix.changed == 'true'
        uses: docker/build-push-action@v6
        with:
          context: backend
          file: backend/${{ matrix.dockerfile }}
          push: true
          platforms: linux/amd64
          tags: |
            ${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ matrix.ecr_repo }}:sha-${{ steps.vars.outputs.sha_short }}
            ${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ matrix.ecr_repo }}:${{ steps.vars.outputs.image_tag }}

      - name: Render task definition (dev) - ${{ matrix.name }}
        if: matrix.changed == 'true'
        id: render
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        with:
          task-definition: ${{ matrix.taskdef }}
          container-name: ${{ matrix.container_name }}
          image: ${{ env.AWS_ACCOUNT_ID }}.dkr.ecr.${{ env.AWS_REGION }}.amazonaws.com/${{ matrix.ecr_repo }}:${{ steps.vars.outputs.image_tag }}

      - name: Deploy to ECS service (dev) - ${{ matrix.name }}
        if: matrix.changed == 'true'
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.render.outputs.task-definition }}
          service: ${{ matrix.ecs_service_dev }}
          cluster: ${{ env.ECS_CLUSTER_DEV }}
          wait-for-service-stability: true
